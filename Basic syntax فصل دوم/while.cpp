#include <iostream>
using namespace std ;

int main ()
{
    // loop 
    // have one block code and counter and loop condition like this 
    // while (condition )
    //{block code and counter }
    // question out put the 1 to 10 number 
    int number = 1 ; // beter you initialize this line  
    while (number <= 10  ){
        cout << "number is : [" << number << "]"  ;
        cout << endl;  
        number++ ; // this is like counter 
    }
    return 0 ; 
}